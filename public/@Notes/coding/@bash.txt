
sudo -i
> whoami --> root
> exit    // Says 'logout' whenever I type 'exit'.
> whoami --> ubuntu
>> su - postgres  // To switch to another user.

sudo service --status-all

du-c9 | sort -h
> Shows what files are consuming the most disk space.


rm -rf node_modules/  //Remove all (recursively) from a folder.
cp ../npm/package.json .   //Copy file from neighboring folder. From Yarn demo video.


lsb_release -a
> Ubuntu 14.04.3 LTS


lsof -i :8080
> Shows what's running on this Port.


$ uname -r   // Gives kernel version
> 4.8.8-c9


dpkg -l  // I was told about this during one of those quick messages as c9 is opening.
> Show all the packages installed.

hostname
> developeralex-ng2-list-001-4317326

vim = Vi IMproved - enhanced vi editor
tree
sudo
nginx
man man


sudo apt-get update


One way to install node & npm:
> sudo apt-get update
> sudo apt-get install nodejs
> sudo apt-get install npm




