import { ResSecDirective } from './res-sec.directive';

describe('ResSecDirective', () => {
  it('should create an instance', () => {
    const directive = new ResSecDirective();
    expect(directive).toBeTruthy();
  });
});
